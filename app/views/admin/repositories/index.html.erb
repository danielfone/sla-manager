
<h2>
  Repositories
</h2>

<%- if @repositories.any? -%>
<table class="table">
  <thead>
    <tr>
      <th>Repository</th>
      <th>Client</th>
      <th>Last Entry</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @repositories.order('last_entry_at NULLS FIRST').each do |repository| %>
      <tr>
        <td><%= link_to repository.name, admin_repository_log_entries_path(repository) %></td>
        <td><%= repository.client_name %></td>
        <td><%= time_ago_or_nil repository.last_entry_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%- else -%>
  <p class="alert alert-info">No repositories found.</p>
<%- end -%>
